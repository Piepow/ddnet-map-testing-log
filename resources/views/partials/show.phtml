<header class="log-header">
    <span class="channel-marker">#</span><span class="channel-name"><?php echoh($log->name) ?></span>
    <div class="channel-topic"><?php echoh($log->topic) ?></div>
</header>

<article class="log">
    <?php foreach($log->messages as $message): ?>
    <div class="msg">
        <div class="msg-left">
            <img class="msg-avatar" src="<?php
                echoh(
                    $assetFetcher->getLink(
                        $config['assets']['mapTestingLogs']['avatars']['subPath'] . $message->author->avatar->id . '.png'
                    )
                ) ?>">
        </div>
        <div class="msg-right">
            <div class="msg-header">
                <span
                    class="msg-user-<?php echoh($message->author->getProminentRole()) ?>"
                    title="<?php echoh($message->author->name .  '#' .  $message->author->discriminator) ?>">
                    <?php echoh($message->author->name) ?>
                </span>
                <span class="msg-date"><?php echoh($message->timestamp->format("m/d/Y g:i A")) ?></span>
            </div>
            <div class="msg-content">
                <?php
                foreach ($message->content as $messageComponent) {
                    echo($messageComponentRenderer->render($messageComponent));
                }
                ?>
            </div>
        </div>
    </div>
    <?php endforeach ?>
</article>
